import PackageDescription

let package = Package(
    name: "Config Validator",
    dependencies: [],
    targets: [
        .target(
            name: "Config Validator",
            dependencies: [])
    ]
)
